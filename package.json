{
  "name": "rodrigoogam-portfolio",
  "private": true,
  "version": "1.0.0",
  "description": "Build tooling for the self-hosted Tailwind CSS used by index.html. The site itself needs no build step to run — this is only for regenerating assets/tailwind.css after editing classes.",
  "scripts": {
    "build:css": "tailwindcss -i ./src/input.css -o ./assets/tailwind.css --minify"
  },
  "devDependencies": {
    "tailwindcss": "^3.4.0"
  }
}
